name: Build documentation

on:
  push:
    branches:
    - master

jobs:

  build:
 
    runs-on: ubuntu-latest
 
    steps:
    - uses: actions/checkout@v2
    - name: build-push
      uses: docker/build-push-action@v1
      with:
        username: ${{ secrets.swannporteil }}
        password: ${{ secrets.moicmoi83 }}
        registry: docker.io
        repository: D:\Documents\Bureau\CloudAwarenessLab
        tag_with_sha: true
